{
  "name": "@merra/site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run validate && next dev",
    "build": "npm run validate && next build",
    "build:sites": "npm run validate && SITES_BUILD=1 opennextjs-cloudflare build && wrangler deploy --dry-run --outdir .worker-dist",
    "check": "npm run validate && next typegen && tsc --noEmit",
    "check:sites": "npm run validate && SITES_BUILD=1 next typegen && tsc --noEmit",
    "validate": "tsx scripts/validate-content.ts"
  },
  "dependencies": {
    "next": "16.2.12",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "react-markdown": "10.1.0",
    "remark-gfm": "4.0.1"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "1.20.2",
    "@types/node": "26.1.2",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "tsx": "4.23.1",
    "typescript": "7.0.2",
    "wrangler": "4.114.0"
  },
  "overrides": {
    "postcss": "8.5.23",
    "sharp": "0.35.3"
  },
  "engines": {
    "node": ">=22"
  }
}
